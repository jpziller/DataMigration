# Normalize line endings to LF in the repo regardless of a contributor's
# platform/core.autocrlf setting -- avoids the whole-file-touched diffs that
# happen when Windows and non-Windows contributors' checkouts disagree.
* text=auto eol=lf

# Generated Excel/Office artifacts are binary -- never diff/merge as text.
*.xlsx binary
*.docx binary
